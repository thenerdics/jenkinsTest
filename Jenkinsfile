#!/usr/bin/env groovy

pipeline {
    agent any
    stages{
        stage('Build') {
            sh '''
                ls
                printenv
                pwd
            '''
        }
    }
}